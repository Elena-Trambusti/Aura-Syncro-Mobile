import { HardwareSettings } from '@/components/hardware/HardwareSettings';

export const metadata = {
  title: 'Hardware | Aura Syncro',
};

export default function HardwareSettingsPage() {
  return <HardwareSettings />;
}
